# Python
.venv/
__pycache__/
*.pyc

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local secrets / data
*.archipelago
death_leaderboard.json
.env
.env.*
# ...but the demo flag file carries no secret and must ship for `build:demo`.
!frontend/.env.demo

# Runtime data + host-dropped assets (keep the folders, ignore their contents)
/data/*
!/data/.gitkeep
/multiworld/*
!/multiworld/.gitkeep

package-lock.json