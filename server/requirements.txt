fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12
pydantic>=2

# Dev-only: needed for server/tests/ and scripts/measure_rss.py, not by the
# app itself. Not required for a plain self-hosted install/run.
pytest>=8
httpx>=0.27
httpx2>=2.0     # starlette.testclient's preferred transport; silences its httpx deprecation warning
psutil>=5.9
